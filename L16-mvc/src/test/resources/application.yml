spring:
  datasource:
    url: jdbc:h2:mem:testdb
    initialization-mode: always
    data: testData.sql
  jpa:
    generate-ddl: false
    hibernate:
      ddl-auto: none
logging:
  level:
    ROOT: DEBUG


