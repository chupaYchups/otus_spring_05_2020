spring:
  datasource:
    url: jdbc:h2:mem:testdb
    initialization-mode: always
    data: testData.sql

  jpa:
    generate-ddl: false
    #generate-ddl: true
    hibernate:
      ddl-auto: none
      #ddl-auto: create-drop

    #show-sql: true

logging:
  level:
    ROOT: DEBUG